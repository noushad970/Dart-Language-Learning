void main() {
  // is operator
//this operator is a bool type datatype if we want to test something and return type is
// true or false than we use it
  dynamic a = 10;
  print("is operator:${a is double}");
  // is! operator
  // this is opposite of is operator
  var x = "noushad";
  print("is! operator:${x is! int}");
}
